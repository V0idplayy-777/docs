---
title: "How Overmind Remembers Context Across All Your Chats"
sidebarTitle: "Memory"
description: "Learn how Overmind uses cross-chat memory and smart context compression to keep conversations coherent, and how to control these behaviors."
---

Overmind can draw on your past conversations as extra context when you start a new chat, so it already knows what you've been working on. It also automatically manages the length of long conversations in the background to keep responses fast and coherent — no manual management required.

## Cross-Chat Memory

When memory is enabled, Overmind draws on your recent chat history and includes it as context whenever you send a new message. This means the AI can recall preferences you've expressed, projects you're working on, or decisions you've already made — even when you open a brand-new chat.

You control this behavior from **Settings → Privacy** using the toggle labeled **Memory** (described in the panel as *"When on, the bot can use prior chats as extra context"*). Memory is **on by default**.

## Smart Context Compression

As a conversation grows very long, Overmind automatically compresses the older portion of the chat into a structured summary. This keeps the conversation within a manageable size without losing the thread of your discussion.

The compression process:

- Triggers automatically once a conversation grows long enough to need it.
- Preserves **key facts**, **decisions**, **user preferences**, **ongoing topics**, and any checklist outcomes from the summarized portion.
- Always keeps the most recent messages verbatim, so the tail of the conversation is fully intact.
- Replaces older messages with a concise summary, seamlessly integrated into the conversation flow.

The compression runs silently in the background — you won't see a spinner or notice any interruption.

## Disabling Memory

To stop Overmind from using past chats as context, go to **Settings → Privacy** and turn off the **Memory** toggle. Once it's off, each new conversation starts fresh, and only the messages in the current chat window are sent to the model.

<Note>
  Context compression is separate from memory. Even with memory turned off, Overmind may compress a very long single conversation to keep it manageable.
</Note>

## Session Context

Within a single open conversation, Overmind keeps the full message history available unless compression is triggered. **Pinned messages are always preserved in context**, regardless of compression — they are stored independently in your browser and are never discarded by the summarization process.
